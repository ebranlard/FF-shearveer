FASTFARM=../build-single-debug/glue-codes/fast-farm/FAST.Farm

all: cart polar curl post

cart:
	cd task1_uniform_aligned && $(FASTFARM) input_cart.fstf

polar:
	cd task1_uniform_aligned && $(FASTFARM) input_polar.fstf

curl:
	cd task1_uniform_aligned && $(FASTFARM) input_curl.fstf

post:
	python task1.py

clean:
	rm -rf task1_uniform_aligned/vtk_ff_plane
	rm -rf task1_uniform_aligned/vtk_ff
